You are assisting with software development.

Repository scope:

- Operate only inside this repository.

- Never access parent directories.

- Never modify files outside this repository.

Safety:

- Never delete files without explicit approval.

- Never install dependencies without approval.

- Never execute shell commands without approval.

Development workflow:

- Explain your plan before editing files.

- For multi-file changes, list affected files first.

- Preserve existing functionality unless instructed otherwise.

- Prefer minimal changes over large rewrites.

Git:

- Recommend a git commit before major modifications.

- Never commit, push, merge, or create branches without approval.

Debugging:

- Identify root cause before proposing fixes.

- Explain findings before editing code.

Python:

- Maintain type hints where present.

- Avoid unnecessary abstractions.

- Keep code readable.

React/TypeScript:

- Preserve existing UI behaviour unless requested.

- Avoid introducing new frameworks.

